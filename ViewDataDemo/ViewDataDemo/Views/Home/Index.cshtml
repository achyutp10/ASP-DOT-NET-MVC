
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
<h2>@ViewData["Message"]</h2>
<h2>@ViewData["CurrentTime"]</h2>

<ul>
    @foreach (string i in (string[])ViewData["FruitsData"])
	{
        <li>@i</li>
	}
</ul>

<ul>
    @foreach (string i in (List<string>)ViewData["SportsList"])
	{
        <li>@i</li>
	}
</ul>

@{ 
    var a = (ViewDataDemo.Models.Enployee)ViewData["Enployee"];
}

<p>@a.EmpID</p>
<p>@a.EmpName</p>
<p>@a.EmpDesignation</p>
